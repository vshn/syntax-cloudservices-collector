apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: exoscale-objectstorage-podmonitor
  namespace: appuio-cloud-reporting
spec:
  podMetricsEndpoints:
    - port: exporter
  selector:
    matchLabels:
      name: exoscale-objectstorage
