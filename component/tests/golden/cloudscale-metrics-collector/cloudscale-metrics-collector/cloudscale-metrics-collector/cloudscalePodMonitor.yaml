apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: cloudscale-podmonitor
  namespace: appuio-cloud-reporting
spec:
  podMetricsEndpoints:
    - port: exporter
  selector:
    matchLabels:
      name: cloudscale
