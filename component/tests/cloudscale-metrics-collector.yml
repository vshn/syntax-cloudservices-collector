parameters:
  billing_collector_cloudservices:
    database:
      name: 'reporting'
      host: 'reporting-db.appuio-reporting.svc'
      parameters: 'sslmode=disable'
      password: 'passw0rd'
      port: 5432
    cloudscale:
      enabled: true
      objectStorage:
        enabled: true
