applications:
  - appuio-cloud-reporting

parameters:
  billing_collector_cloudservices:
    appuioManaged:
      enabled: true
      tenant: ${cluster:name}
      salesOrder: "12345"
    cloudscale:
      enabled: true
